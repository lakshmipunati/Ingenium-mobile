import { test } from "./test.reducer";
import { login } from "./login.reducer";
import { dataTab, lookupData, udfSelected } from "./data.reducer";

export const reducer = {
    test,
    login,
    dataTab,
    lookupData,
    udfSelected
}